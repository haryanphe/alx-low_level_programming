#include <stdio.h>
/**
 * main - putchar alphabet in lower case
 * return 0
 */
int main(void)
{
	char hc;

	for
		(hc = 'a'; hc <= 'z';
		 hc++);
	{
	putchar(hc);
	}
	{
	putchar('\n');
	}

	return (0);
}
