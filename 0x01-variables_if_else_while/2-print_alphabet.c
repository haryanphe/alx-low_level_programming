#include <stdio.h>
/**
 * main - putchar alphabet in lower case
 * return 0
 */
int main(void)
{
	char lc;

	for
		(lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	{
		putchar('\n');
	}
	return (0);
}
