#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
*/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		putchar(num);
	}
	if (num = < 10)
	{
		putchar(',');
	}
	if (num = < 10)
		putchar(' ');

	putchar('\n')

	return (0);
}
