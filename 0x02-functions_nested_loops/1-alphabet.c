#include <stdio.h>
#include "main.h"
/**
  * main - Entry point
  * function
  * Return: (0)
  */
int main(void)
{
	int x;
{
	for
	(x = 'a' ; x < = 'z' ;, x++);
	_putchar(x0);
}
	_putchar('\n');

	return (0);
}
