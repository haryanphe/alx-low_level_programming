#include "main.h"
/**
*int _isalpha(int c) - check for alphabetic character
*
*Return: 1 if c is a letter,otherwise 0
*/
int _isalpha(int c)
{
if (c >= 'a', c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
